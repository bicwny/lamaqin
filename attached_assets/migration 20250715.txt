Phase 1 Scope Should Actually Include:
1. Core Navigation & Layout

app/(tabs)/_layout.tsx - Tab bar styling, spacing, shadows
app/_layout.tsx - App-wide theme configuration
components/PageTemplate.tsx - Base page structure
components/ModalTemplate.tsx - Modal styling patterns
2. Authentication Flow

app/auth/login.tsx - Form styling, button patterns
app/auth/email-verification.tsx - Status indicators, text styles
app/profile-setup.tsx - Multi-step form patterns
3. Main Feature Screens

app/(tabs)/index.tsx - Daily dashboard
app/(tabs)/study.tsx - Course cards, progress bars
app/(tabs)/practice.tsx - Practice cards, status badges
app/(tabs)/mindfulness.tsx - Meditation UI patterns
app/(tabs)/stats.tsx - Charts, statistical displays
4. Detail & Modal Screens

app/practice-detail/[practiceId].tsx (already identified)
app/course-detail/[courseId].tsx - Course layout patterns
app/modals/meditation-record.tsx - Form modals
app/modals/custom-record.tsx - Input patterns
app/lesson-viewer.tsx - Content display patterns
5. Shared Components

components/ThemedText.tsx vs DesignSystem.typography
components/ThemedView.tsx vs DesignSystem.colors
All UI components in /components directory
Key Issues Across the Entire App
1. Inconsistent Color Usage

Some files import Colors.ts, others use DesignSystem.colors
Hardcoded hex values scattered throughout
Buddhist semantic colors not systematically applied
2. Typography Chaos

Mix of hardcoded font sizes (12, 14, 16, 18, 20, 24px)
Inconsistent font weights and line heights
No systematic heading hierarchy
3. Spacing Inconsistencies

Random padding/margin values (4, 8, 12, 16, 20, etc.)
No consistent spacing scale application
Different spacing patterns per developer/component
4. Component Style Duplication

Similar button styles redefined everywhere
Card styles inconsistent across components
Progress bars styled differently per use case
5. Shadow & Elevation Patterns

Different shadow configurations scattered
No consistent elevation system
Some using StyleSheet, others inline
Comprehensive Migration Approach
Phase 1: Foundation (Week 1)

Expand DesignSystem.ts to cover ALL patterns found app-wide
Create migration utilities
Document every hardcoded value across all files
Phase 2: Core Infrastructure (Week 2)

Migrate shared components (ThemedText, ThemedView, etc.)
Update template components (PageTemplate, ModalTemplate)
Fix navigation styling (_layout.tsx files)
Phase 3: Feature Screens (Week 3-4)

Migrate tab screens one by one
Update detail screens (practice-detail, course-detail)
Fix modal screens
Phase 4: Polish & Cleanup (Week 5)

Remove Colors.ts entirely
Clean up hardcoded styles
Performance optimization
The Real Challenge
The challenge isn't just practice-detail - it's that every single screen probably has:

Unique hardcoded values that need to become design tokens
Buddhist-specific colors that need semantic names
Inconsistent component patterns that need standardization
That's why Phase 1 is so critical - we need to discover and systematize every style pattern used across the entire app before we can migrate anything safely.

Would you like me to create a more comprehensive audit plan that covers specific files and the patterns we'd expect to find in each area of the app?