# 佛教修行追踪App - MVP技术规范

## 📋 文档使用指南

### **文档目的**
本文档是佛教修行追踪App的主控技术规范，专注于UI组件、业务逻辑和数据模型关系，用于指导MVP开发实施。

### **给下次对话Claude的指引**
- 本文档是完整的技术实现规范
- 重点关注"🔄 更新追踪仪表板"了解最新变更
- 优先实现标记为"🔥 MVP必需"的功能
- 所有界面规范都包含完整的交互逻辑，可直接转换为代码

### **给Replit Agent的快速指引**
- **开发重点**: 认证系统 → 修行记录 → 基础统计
- **技术栈**: Expo React Native + Supabase + TypeScript
- **数据库**: 12个核心表，完整SQL脚本已提供
- **关键特性**: 实时同步修行记录，异步处理统计数据

---

## 🔄 更新追踪仪表板

| 模块 | 最后更新 | 状态 | 开发优先级 |
|------|----------|------|------------|
| 认证系统 (0.1-0.8) | 2025-07-02 14:30 | ✅ 完整 | 🔥 P0 - MVP必需 |
| 修行记录 (1.1-1.7) | 2025-07-02 10:20 | ✅ 完整 | 🔥 P0 - 核心业务 |
| 闻思学习 (2.1-2.3) | 2025-07-01 16:20 | ✅ 完整 | 🟡 P1 - 重要功能 |
| 心性记录 (3.1-3.4) | 2025-07-02 13:45 | ✅ 完整 | 🟡 P1 - 差异化功能 |
| 统计模块 (4.1-4.3) | 2025-07-01 15:15 | ✅ 完整 | 🟢 P2 - 增值功能 |
| 个人中心 (5.1-5.8) | 2025-07-02 14:30 | ✅ 完整 | 🟡 P1 - 用户管理 |

---

## 📋 产品简介

### **应用概述**
佛教修行追踪App是一个综合性移动应用，支持修行者记录和追踪完整的修行活动，涵盖戒定慧三学的全面实践。

### **核心功能模块**
1. **闻思学习** - 课程听讲记录和进度管理
2. **修行记录** - 修法计数、观修时间、主题管理
3. **心性观照** - 善恶心记录和趋势分析  
4. **统计分析** - 全面的修行数据统计和可视化
5. **个人中心** - 用户管理、设置和数据导出

### **主界面导航结构**
```
📚 闻思  |  📿 修行  |  💝 心性  |  📊 统计  |  👤 个人
```

---

## 🛠️ 技术栈

### **前端框架**
- **Expo React Native** (~53.0.9) - 跨平台移动应用开发
- **TypeScript** - 类型安全和代码质量
- **Expo Router** - 基于文件的路由系统

### **后端服务**
- **Supabase** - PostgreSQL数据库 + 实时同步 + 认证
- **Supabase Auth** - 邮箱验证和用户管理
- **Supabase Storage** - 文件存储（如需要）

### **状态管理**
- **React Context** + **useState** - 轻量级状态管理
- **Async Storage** - 本地数据缓存和离线支持

### **关键依赖**
```json
{
  "@supabase/supabase-js": "^2.39.0",
  "@react-navigation/native": "^6.1.9", 
  "@react-navigation/bottom-tabs": "^6.5.11",
  "expo-notifications": "~0.25.0",
  "react-native-reanimated": "~3.6.0"
}
```

---

## 🗄️ 数据模型

### **核心表结构 (12张表)**

#### **用户管理**
```sql
-- 用户信息表
users (
  id UUID PRIMARY KEY,
  dharma_name VARCHAR(100),           -- 法名
  email VARCHAR(255) UNIQUE,          -- 邮箱
  location VARCHAR(100),              -- 常住地
  practice_years INTEGER,             -- 修行年限
  class_name VARCHAR(100),            -- 班级
  main_practices JSONB,               -- 修行偏好
  notification_settings JSONB,        -- 通知设置
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)
```

#### **修行体系**
```sql
-- 修行主题表
themes (
  id UUID PRIMARY KEY,
  name VARCHAR(100) NOT NULL,         -- 主题名称：五加行、金刚萨埵法会
  description TEXT,                   -- 主题描述
  type VARCHAR(50) NOT NULL,          -- 类型：foundation, puja, meditation
  created_at TIMESTAMP
)

-- 修法表
practices (
  id UUID PRIMARY KEY,
  name VARCHAR(100) NOT NULL,         -- 修法名称：顶礼、百字明
  type VARCHAR(50) NOT NULL,          -- 类型：count(计数) 或 time(时间)
  unit VARCHAR(20) NOT NULL,          -- 单位：次 或 分钟
  description TEXT,
  created_at TIMESTAMP
)

-- 主题修法关系表
theme_practices (
  id UUID PRIMARY KEY,
  theme_id UUID REFERENCES themes(id),
  practice_id UUID REFERENCES practices(id),
  target_count INTEGER NOT NULL,      -- 目标数量：100000
  is_required BOOLEAN DEFAULT true,
  created_at TIMESTAMP
)
```

#### **课程体系**
```sql
-- 课程表
courses (
  id UUID PRIMARY KEY,
  name VARCHAR(100) NOT NULL,         -- 课程名称：《入菩萨行论》
  total_lessons INTEGER NOT NULL,     -- 总课数：201
  teacher VARCHAR(100),               -- 授课上师
  description TEXT,
  created_at TIMESTAMP
)

-- 课程章节表
course_lessons (
  id UUID PRIMARY KEY,
  course_id UUID REFERENCES courses(id),
  lesson_number INTEGER NOT NULL,     -- 课次：1, 2, 3...
  title VARCHAR(200) NOT NULL,        -- 课程标题
  content_summary TEXT,               -- 内容摘要
  created_at TIMESTAMP
)
```

#### **用户活动数据**
```sql
-- 用户修行项目表
user_practice_projects (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  theme_id UUID REFERENCES themes(id),
  practice_id UUID REFERENCES practices(id),
  target_count INTEGER NOT NULL,      -- 目标数量
  start_date DATE,                    -- 开始日期
  target_end_date DATE,               -- 目标完成日期
  current_count INTEGER DEFAULT 0,    -- 当前进度
  daily_target INTEGER DEFAULT 0,     -- 每日目标
  status VARCHAR(20) DEFAULT 'not_started', -- not_started, active, completed
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)

-- 每日修行记录表
daily_records (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  practice_project_id UUID REFERENCES user_practice_projects(id),
  record_date DATE NOT NULL,          -- 记录日期
  count INTEGER NOT NULL,             -- 完成数量
  notes TEXT,                         -- 备注
  created_at TIMESTAMP
)

-- 观修记录表
meditation_records (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  practice_id UUID REFERENCES practices(id),
  record_date DATE NOT NULL,
  session_number INTEGER NOT NULL,    -- 第几座 (1-92)
  duration_minutes INTEGER NOT NULL,  -- 观修时长(分钟)
  session_attempt INTEGER DEFAULT 1,  -- 这座的第几次 (1-3+)
  method VARCHAR(100),                -- 观修方法
  created_at TIMESTAMP
)

-- 听课记录表
study_records (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  course_id UUID REFERENCES courses(id),
  lesson_id UUID REFERENCES course_lessons(id),
  study_date DATE NOT NULL,
  study_count_for_lesson INTEGER DEFAULT 1, -- 该课的第几次学习
  created_at TIMESTAMP
)

-- 观心记录表
mindfulness_records (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  record_date DATE NOT NULL,
  record_time TIME NOT NULL,
  mind_type VARCHAR(10) NOT NULL,     -- 'good'(善心) 或 'bad'(恶心)
  description TEXT,                   -- 具体描述
  created_at TIMESTAMP
)

-- 一次性修行记录表
one_time_records (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  record_date DATE NOT NULL,
  practice_type VARCHAR(50) NOT NULL, -- 计数修法、观修记录、功课诵读、持戒记录、其他修行
  practice_name VARCHAR(100) NOT NULL,
  count_or_duration VARCHAR(50) NOT NULL,
  notes TEXT,
  created_at TIMESTAMP
)

-- 持戒记录表
precept_records (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  precept_type VARCHAR(50) NOT NULL,  -- 八关斋戒、菩萨戒、诵戒、荟供、密乘戒
  record_date DATE NOT NULL,
  notes TEXT,
  created_at TIMESTAMP
)
```

---

## 📱 界面功能规范

### **用户认证系统**

#### **0.1 登录页面**
- **UI组件**: 邮箱输入框、密码输入框、登录按钮、注册链接、忘记密码链接
- **交互逻辑**: 表单验证 → Supabase认证 → 成功后跳转主应用
- **错误处理**: 显示认证错误信息，网络错误重试

#### **0.2 注册页面**  
- **UI组件**: 法名输入框(可选)、邮箱输入框、密码输入框、确认密码输入框、注册按钮
- **交互逻辑**: 表单验证 → Supabase注册 → 跳转邮箱验证页面
- **验证规则**: 邮箱格式、密码长度≥6位、密码确认匹配

#### **0.3 忘记密码页面**
- **UI组件**: 邮箱输入框、发送重置邮件按钮
- **交互逻辑**: 发送重置邮件 → 显示发送成功页面
- **业务逻辑**: 调用Supabase重置密码API

#### **0.4 忘记密码 - 发送成功页面**
- **UI组件**: 成功提示、重新发送按钮、返回登录按钮
- **交互逻辑**: 重新发送有60秒冷却时间

#### **0.5 密码重置页面**
- **UI组件**: 新密码输入框、确认密码输入框、重置按钮
- **交互逻辑**: 通过邮箱链接进入，验证token → 设置新密码 → 返回登录

#### **0.6 邮箱验证等待页面**
- **UI组件**: 验证步骤说明、重新发送按钮、已验证去登录按钮
- **交互逻辑**: 60秒重发限制，引导用户完成邮箱验证

#### **0.7 首次登录欢迎页面**
- **UI组件**: 欢迎信息、应用功能介绍、完善资料按钮、跳过按钮
- **交互逻辑**: 验证成功后首次进入，可选择完善资料或直接进入应用

#### **0.8 个人资料设置页面**
- **UI组件**: 基本信息表单、修行偏好多选、感兴趣课程多选、保存按钮
- **数据字段**: dharma_name, location, practice_years, class_name, main_practices, interested_courses
- **交互逻辑**: 数据保存到users表，完成后进入主应用

### **📚 闻思学习模块**

#### **2.1 闻思主页**
- **UI组件**: 我的课程卡片列表、本周闻法统计、浏览全部课程按钮、今日听课按钮
- **数据显示**: 每个课程显示当前进度(第X课/总课数)、进度百分比、本周听课次数
- **交互逻辑**: 点击课程卡片 → 课程详情页面，点击[继续学习] → 快速听课登记

#### **2.2 快速听课登记**
- **UI组件**: 课程选择下拉框、课次选择下拉框、完成按钮
- **业务逻辑**: 记录到study_records表，自动更新课程进度，智能推进到下一课
- **数据关系**: user_id + course_id + lesson_id + study_date

#### **2.3 课程详情页面** 
- **UI组件**: 课程信息展示、学习进度条、最近学习记录列表、下一课显示
- **数据查询**: 从study_records查询该用户的学习历史
- **功能**: 支持重复听课，每课可听多次并独立记录

### **📿 修行记录模块**

#### **1.1 修行主页**
- **UI组件**: 今日功课列表、我的主题卡片、添加修法按钮、生成分享按钮
- **今日功课显示**: 修法名称、当前进度/今日目标、完成状态图标、[完成]和[自定义记录]按钮
- **主题卡片**: 主题名称、完成百分比、进行中修法列表、未启动修法列表、[管理主题]按钮

#### **1.2 修法记录交互**
- **[完成]按钮逻辑**: 直接完成当日目标数量，更新daily_records和user_practice_projects.current_count
- **[自定义记录]按钮**: 弹出输入框，用户输入任意数量，支持超额完成
- **进度计算**: current_count / target_count，实时更新显示

#### **1.3 自定义记录弹窗**
- **UI组件**: 当前进度显示、今日目标显示、剩余数量显示、数量输入框、确认取消按钮
- **输入验证**: 数字验证，最小值0，可以超过今日目标
- **数据更新**: 插入daily_records记录，更新user_practice_projects.current_count

#### **1.4 观修记录**
- **UI组件**: 日期选择器、座次选择下拉框、观修时长输入列表、增加座按钮、保存按钮
- **业务逻辑**: 支持一日多座记录，每座独立记录时长，自动计算总时长
- **验证规则**: 总时长≥30分钟 或 每座≥15分钟且有2座以上
- **数据结构**: session_number(1-92), duration_minutes, session_attempt

#### **1.5 添加修法功能**
- **四种添加方式**:
  1. **加入主题**: 选择预设主题(五加行、金刚萨埵法会等)，可选择部分修法启动
  2. **单独添加修法**: 从practices表选择单个修法，设置个人目标
  3. **添加每日功课**: 添加无具体目标的日常功课
  4. **一次性修行记录**: 记录临时修行活动
- **数据操作**: 插入user_practice_projects记录，status设为'active'

#### **1.6 主题管理系统**
- **主题选择流程**: 显示主题包含的修法列表 → 选择要启动的修法 → 设定时间目标 → 创建项目
- **渐进式启动**: 可以只启动部分修法，其他修法稍后在主题管理中启动
- **主题管理界面**: 
  - 进行中修法: 显示进度百分比、开始日期、目标日期
  - 未启动修法: 显示[立即启动]按钮
  - 已完成修法: 显示完成日期和统计

#### **1.7 一次性修行记录**
- **五大类型**: 计数修法、观修记录、功课诵读、持戒记录、其他修行
- **UI组件**: 类型选择、修法/活动选择下拉框、数量/时长输入、备注输入、保存按钮
- **数据存储**: one_time_records表，不影响正式项目进度但计入总体统计

### **💝 心性记录模块**

#### **3.1 观心主页**
- **UI组件**: 快速记录按钮([🤍 善心] [🖤 恶心])、今日统计显示、查看统计按钮
- **快速记录逻辑**: 点击按钮 → 立即插入mindfulness_records → 更新今日计数显示
- **今日统计**: 善心次数、恶心次数、善心比例、趋势箭头(↗️↘️)

#### **3.2 观心统计**
- **UI组件**: 过去7天数据列表、可视化图表按钮
- **数据展示**: 每日善心比例、趋势变化指示
- **数据查询**: 从mindfulness_records按日期聚合统计

#### **3.3 观心可视化**
- **UI组件**: 7天可视化图表(🤍🖤符号显示)、月度统计摘要
- **图表逻辑**: 每天按比例显示善心恶心符号，直观显示趋势
- **月度统计**: 平均善心比例、连续记录天数、最佳表现日期

#### **3.4 观心日记**
- **UI组件**: 日期选择、善心记录列表、恶心记录列表、今日反思输入框
- **记录详情**: 时间戳、具体事件描述
- **反思功能**: 每日总结和修行心得记录

### **📊 统计模块**

#### **4.1 统计主页**
- **UI组件**: 主题完成度卡片、闻思统计、心性统计、持戒统计、月度总结按钮
- **主题完成度**: 每个主题显示总进度百分比，细分每个修法的进度
- **观修特殊显示**: 第X座/共92座，总时长，缺失座次提醒
- **数据聚合**: 从相关记录表实时计算或使用缓存

#### **4.2 今日修行总结**
- **UI组件**: 每日功课完成清单、一次性修行列表、闻思记录、心性记录、今日统计汇总
- **自动生成**: 汇总当天所有修行活动
- **数据来源**: daily_records + one_time_records + study_records + mindfulness_records

#### **4.3 月度总结**
- **UI组件**: 月度成就、进步亮点、需要改进、下月目标设定
- **数据分析**: 对比上月数据，计算增长趋势
- **智能洞察**: 基于数据生成修行建议

### **👤 个人中心模块**

#### **5.1 个人主页**
- **UI组件**: 用户信息卡片、应用设置列表、数据管理列表、账户安全列表、帮助支持列表、退出登录按钮
- **用户信息**: 显示dharma_name、修行年限、常住地、班级、注册时间
- **功能入口**: 各种设置和管理功能的入口

#### **5.2 编辑个人资料**
- **UI组件**: 完整的个人信息编辑表单，包括基本信息、联系方式、修行偏好、修行时间、提醒设置
- **数据绑定**: 从users表加载现有数据，支持实时更新
- **表单验证**: 字段格式验证，保存前确认

#### **5.3 安全退出确认**
- **UI组件**: 确认对话框、退出前操作选项(备份数据、导出数据)、下次登录设置、操作按钮
- **退出逻辑**: 可选数据备份 → Supabase signOut → 清理本地缓存 → 跳转登录页

#### **5.4 修改密码**
- **UI组件**: 当前密码输入、新密码输入、确认密码输入、密码建议提示、安全提醒、确认按钮
- **验证逻辑**: 当前密码验证 → 新密码强度检查 → 密码匹配验证
- **安全措施**: 修改成功后发送邮件通知、强制其他设备退出登录

#### **5.5 删除账户**
- **UI组件**: 警告信息、删除影响说明、删除前建议、安全验证(密码+确认文本)、确认删除按钮
- **删除流程**: 密码验证 → 确认文本验证 → 数据备份提醒 → 执行删除 → 清理本地数据

#### **5.6 更换邮箱**
- **UI组件**: 当前密码验证、新邮箱输入、验证流程说明、发送验证码按钮
- **验证流程**: 新邮箱发送验证码 → 当前邮箱发送确认通知 → 输入验证码完成更换

#### **5.7 每日分享生成**
- **UI组件**: 自动生成格式、自定义编辑、分享模板、复制/生成图片/分享按钮
- **内容生成**: 汇总当日所有修行记录，生成标准化分享文案
- **模板系统**: 提供文字版和图片版分享模板

#### **5.8 通知设置**
- **UI组件**: 修行提醒设置、进度提醒设置、观心提醒设置、闻思提醒设置、成就提醒设置
- **提醒类型**: 早晚课提醒、目标完成提醒、观心定时提醒、课程更新通知、成就解锁提醒
- **设置存储**: 保存到users.notification_settings字段

---

## ⚙️ 核心业务逻辑

### **修行记录业务规则**

#### **计数类修法记录**
```typescript
interface CountPracticeRecord {
  practiceId: string;
  count: number;
  recordDate: Date;
}

// 记录逻辑
function recordCountPractice(data: CountPracticeRecord) {
  // 1. 插入daily_records
  // 2. 更新user_practice_projects.current_count
  // 3. 检查是否完成目标(current_count >= target_count)
  // 4. 触发异步统计更新
}
```

#### **观修记录业务规则**
```typescript
interface MeditationSession {
  sessionNumber: number;      // 1-92
  durationMinutes: number;   // 本次时长
  sessionAttempt: number;    // 该座的第几次
}

// 验证规则
function validateMeditationSession(sessions: MeditationSession[]) {
  const totalDuration = sessions.reduce((sum, s) => sum + s.durationMinutes, 0);
  const validSessions = sessions.filter(s => s.durationMinutes >= 15);
  
  // 规则: 总时长≥30分钟 或 每座≥15分钟且有2座以上
  return totalDuration >= 30 || (validSessions.length >= 2);
}
```

#### **主题启动逻辑**
```typescript
// 渐进式主题启动
function joinTheme(themeId: string, selectedPractices: string[]) {
  // 1. 查询theme_practices获取主题包含的修法
  // 2. 根据用户选择创建user_practice_projects
  // 3. 未选择的修法保持not_started状态
  // 4. 可以后续在主题管理中启动
}
```

### **数据同步策略**

#### **实时同步数据 (Critical)**
- 用户认证状态变更
- 修行记录 (daily_records, meditation_records)
- 观心记录 (mindfulness_records)
- 用户修行项目状态 (user_practice_projects)

#### **异步同步数据 (Supporting)**
- 统计报告和图表数据
- 成就检查和里程碑
- 进度百分比计算
- 分享内容生成

#### **缓存策略**
```typescript
// 统计数据缓存1小时
const CACHE_DURATION = {
  dailyStats: 5 * 60 * 1000,      // 5分钟
  weeklyStats: 30 * 60 * 1000,    // 30分钟  
  monthlyStats: 60 * 60 * 1000,   // 1小时
  achievements: 24 * 60 * 60 * 1000 // 24小时
};
```

### **错误处理策略**

#### **网络错误处理**
- 实时数据: 3次重试，失败后保存到本地队列
- 异步数据: 静默重试，用户无感知
- 离线模式: 核心功能本地存储，联网后同步

#### **数据一致性保证**
- 乐观更新: UI立即响应，后台同步
- 冲突解决: 时间戳优先策略
- 数据校验: 关键操作前后数据校验

---

## 🔗 组件依赖关系

### **数据流图**
```
User Actions → UI Components → Business Logic → Data Layer → Supabase

认证状态 ← AuthContext ← Supabase Auth
修行记录 → PracticeService → user_practice_projects, daily_records
统计数据 ← StatsService ← 聚合查询 ← 各记录表
```

### **模块依赖关系**
```
修行记录模块 ←→ 统计模块 (数据消费)
认证模块 → 所有功能模块 (权限控制)
个人中心 ←→ 所有模块 (设置影响)
心性模块 → 统计模块 (数据汇总)
闻思模块 → 统计模块 (进度统计)
```

### **关键API接口**
```typescript
// 修行记录
POST /api/practices/record
PUT /api/practices/projects/{id}/progress

// 统计查询  
GET /api/stats/daily/{date}
GET /api/stats/monthly/{year-month}

// 用户管理
PUT /api/users/profile
POST /api/auth/change-password
```

---

## 🚀 实施优先级

### **Phase 1: 认证+核心记录 (2-3周)**
1. **Supabase项目设置** - 数据库、认证、RLS策略
2. **认证系统** - 登录、注册、邮箱验证、密码重置
3. **基础修行记录** - 计数类修法记录、简单进度显示
4. **数据同步** - 实时记录同步、基础错误处理

### **Phase 2: 核心功能完善 (2-3周)**
1. **观修系统** - 92座观修记录、时长验证
2. **主题管理** - 主题加入、渐进式启动
3. **闻思功能** - 课程记录、听课统计
4. **观心记录** - 善恶心记录、基础统计

### **Phase 3: 高级功能 (2周)**
1. **详细统计** - 月度报告、趋势分析、可视化
2. **一次性记录** - 临时修行活动记录
3. **分享系统** - 每日总结生成、多格式分享
4. **个人中心** - 设置管理、数据导出

### **Phase 4: 优化发布 (1周)**
1. **性能优化** - 异步处理、缓存策略
2. **离线支持** - 关键功能离线可用
3. **推送通知** - 修行提醒、成就通知
4. **错误处理** - 完善的错误处理和用户提示

---

## 📋 开发检查清单

### **环境准备**
- [ ] 创建Supabase项目
- [ ] 执行SQL初始化脚本
- [ ] 配置Supabase认证设置
- [ ] 设置Expo开发环境
- [ ] 配置环境变量

### **Phase 1 关键里程碑**
- [ ] 用户可以注册、验证邮箱、登录
- [ ] 用户可以记录基础修法(顶礼、百字明等)
- [ ] 数据能正确保存到Supabase
- [ ] 基础进度统计正常显示
- [ ] 应用在iOS/Android设备正常运行

### **数据完整性验证**
- [ ] 所有表的外键关系正确
- [ ] RLS策略保证数据安全
- [ ] 基础数据(themes, practices, courses)正确加载
- [ ] 用户修行记录数据流完整

---

*本规范为MVP开发的完整技术指导，包含所有必要的UI组件、业务逻辑和数据模型定义。开发团队可基于此规范直接进行技术实施。*